# Database (Production requires SSL)
DATABASE_URL="postgresql://user:password@host:port/dbname?sslmode=require"

# Authentication
JWT_SECRET="generate-a-long-secure-random-string-here"
REFRESH_TOKEN_SECRET="another-secure-random-string"

# Infrastructure
PORT=3000
FRONTEND_URL="https://app.dataspaceconnect.com"

# Email (SMTP)
SMTP_HOST="smtp.provider.com"
SMTP_PORT=587
SMTP_USER="user@provider.com"
SMTP_PASS="securepassword"
SMTP_FROM="noreply@dataspaceconnect.com"
